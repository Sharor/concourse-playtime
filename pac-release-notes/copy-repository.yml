platform: linux

image_resource:
  type: docker-image
  source: {repository: microsoft/dotnet}

inputs:
- name: repository

outputs:
  - name: release-notes

run:
  path: sh
  args:
  - -exc
  - |
    ls
    cp -r repository/dotnet-testfolder ./release-notes/dotnet-testfolder
    cp repository/pac-release-notes/Dockerfile ./release-notes/Dockerfile

