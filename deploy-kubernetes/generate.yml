platform: linux

image_resource:
  type: docker-image
  source: {repository: sharor/kubectl}

inputs:
- name: repository

outputs: 
- name: kubernetes-yml

run:
  path: repository/deploy-kubernetes/genscript.sh 



