platform: linux

image_resource:
  type: docker-image
  source: {repository: microsoft/dotnet}

inputs:
- name: dotnet-test


run:
  path: sh
  args:
  - -exc
  - |
    ls
    dotnet restore 
    cd dotnet-test
    git add .
    git commit -m "restore dotnet project"    
