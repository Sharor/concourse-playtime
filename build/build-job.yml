platform: linux

image_resource:
  type: docker-image
  source: {repository: microsoft/dotnet}

inputs:
- name: dotnet-test
- name: dotnet-pipeline
outputs:
  - name: build

run:
  path: dotnet-test/build/build.sh






