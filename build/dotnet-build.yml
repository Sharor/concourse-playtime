platform: linux

image_resource:
  type: docker-image
  source: {repository: microsoft/dotnet}

inputs:
- name: dotnet-test
- name: dotnet-restored

run:
  path: sh
  args:
  - -exc
  - |
    ls
    dotnet build 
